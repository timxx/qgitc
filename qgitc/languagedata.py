# -*- coding: utf-8 -*-


from typing import Dict


def loadCppData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadJSData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadShellData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadPHPData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadQMLData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadPythonData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadRustData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadJavaData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadCSharpData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadGoData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadVData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadSQLData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadJSONData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadCSSData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadTypeScriptData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadVexData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadCMakeData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadMakeData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass


def loadYAMLData(types: Dict[str, str], keywords: Dict[str, str], builtin: Dict[str, str], literals: Dict[str, str], others: Dict[str, str]):
    pass
